const LoadingMain = () => {
  return (
    <div className='flex items-center justify-center h-screen w-screen'>
      <div className='spinner-border animate-spin inline-block w-16 h-16 border-4 rounded-full' role='status'></div>
    </div>
  );
};

export default LoadingMain;
